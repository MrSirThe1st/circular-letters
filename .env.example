# Shared defaults - copy to .env.local and fill in your values
# These are used as fallbacks in development

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here

# API (if using separate API layer)
API_URL=http://localhost:3000/api
